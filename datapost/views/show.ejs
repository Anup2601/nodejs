<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>show users</title>
    <style>
        table,tr,td{
            border: 1px solid red;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>id</th>
            <th>username</th>
            <th>email</th>
        </tr>
         <% for(user of users) { %>
         <tr>
            <td><%=user.id%> </td>
            <td><%=user.username%> </td>
            <td><%=user.email%> </td>
            <td>
                <form method="GET" action="/show/<%=user.id%>/edit">
                    <button>edite user</button>
                </form>
            </td>
            <td>
                <form method="GET" action="/show/<%=user.id%>/add">
                    <button>add user</button>
                </form>
            </td><td>
                <form method="GET" action="/show/<%=user.id%>/delete">
                    <button>delete user</button>
                </form>
            </td>
         </tr>
         <% } %>
    </table>
</body>
</html>