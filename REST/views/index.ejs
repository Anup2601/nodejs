<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>quary</title>
    <link rel="stylesheet" href=" /style.css">
</head>
<body>
    <h1>QUARY</h1>
    <% for(let post of posts) { %>
        <div class="post">
            <p>podt id:<%= post.id %></p>
            <h2 class="name">@<%= post.name %></h2>
            <h3 class="content"><%= post.content %></h3>
            <br><br>
            <a href="http://localhost:8080/post/<%= post.id  %>">see in detail</a>
            <a href="http://localhost:8080/post/<%= post.id %>/edit">   edit post</a><br><br>
            <form method="post" action="/post/<%= post.id %>?_method=DELETE">
                 <button>DELETE post</button>
            </form>
        </div>
    <% } %>

    <br>
    <br><br>
    <a href="http://localhost:8080/post/new">creat new post</a>
    <br><br>
     
</body>
</html>